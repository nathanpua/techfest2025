// Configuration file for Exposé extension
// IMPORTANT: For production, replace these placeholder values with actual keys
// using a secure build process that doesn't expose them in source code

const config = {
  // Supabase credentials
  SUPABASE_URL: "SUPABASE_URL_PLACEHOLDER",
  SUPABASE_KEY: "SUPABASE_KEY_PLACEHOLDER",
  
  // Website URL
  WEBSITE_URL: "https://exposeai.netlify.app/",
  
  // OpenAI API key
  OPENAI_API_KEY: "OPENAI_API_KEY_PLACEHOLDER"
};

// Export the configuration
export default config; 